node_modules/
.env
*.log
.DS_Store

# Лок-файл репозиторий никогда не хранил, и сборка на Railway рассчитана
# на его отсутствие: nixpacks без него зовёт npm install, а с ним — npm ci.
# Если захотите закрепить версии зависимостей — уберите эту строку и
# закоммитьте лок-файл отдельно, осознанно.
package-lock.json
