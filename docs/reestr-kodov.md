# Реестр кодов

Всё, что вписано в исходники жёстко: адреса, методы, ключи окружения, имена
листов, номера строк и числовые пределы. Один список, чтобы не искать по коду.

Устройство сервиса — в [`roadmap.md`](roadmap.md), порядок правок — в
[`obnovlenie.md`](obnovlenie.md).

> Проверено по коду на коммите `90cd27d`. Если правите значение — правьте и
> здесь: расхождение этого файла с кодом хуже, чем его отсутствие.

---

## 1. Хосты

| Константа | Адрес | Переопределяется | Файл |
|---|---|---|---|
| `WB_СТАТ` | `https://statistics-api.wildberries.ru` | нет | `wb-ozon.js:18` |
| `WB_АНАЛИТИКА` | `https://seller-analytics-api.wildberries.ru` | `WB_ANALYTICS_URL` | `wb-ozon.js:19` |
| `WB_МАРКЕТ` | `https://marketplace-api.wildberries.ru` | нет | `wb-ozon.js:20` |
| `WB_КОНТЕНТ` | `https://content-api.wildberries.ru` | `WB_CONTENT_URL` | `wb-ozon.js:21` |
| `OZ` | `https://api-seller.ozon.ru` | `OZON_API_URL` | `wb-ozon.js:356` |
| `ЯМ` | `https://api.partner.market.yandex.ru` | нет | `yandex-lamoda.js:10` |
| `LM_RPC` | `https://api-seller.lamoda.ru/rpc` | нет | `yandex-lamoda.js:145` |
| `LM_B2B` | `https://b2b-api.lamoda.ru` | нет | `yandex-lamoda.js:146` |

Три адреса вынесены в окружение **только ради проб**: повадки методов — пределы
периода, пагинацию, лимиты — на живом API не проверить. В бою их не задают.

Функция `подставной(у)` (`wb-ozon.js:16`) узнаёт подставной адрес по
`127.0.0.1` или `localhost` и на нём обнуляет паузы. Выставь кто-нибудь
`OZON_API_URL` на боевой хост — и паузы исчезли бы разом у отправлений и
возвратов, поэтому проверка именно на localhost, а не «задана ли переменная».

---

## 2. Методы API

### Wildberries

| Метод | Хост | Задача | Строка |
|---|---|---|---|
| `GET /api/v1/supplier/orders?dateFrom=…&flag=0` | статистика | Заказы ВБ FBO | `wb-ozon.js:298` |
| `GET /api/v1/supplier/sales?dateFrom=…&flag=0` | статистика | признак выкупа | `wb-ozon.js:300` |
| `POST /api/analytics/v1/stocks-report/wb-warehouses` | аналитика | FBO остатки ВБ | `wb-ozon.js:73` |
| `GET /api/v1/analytics/goods-return?dateFrom=…&dateTo=…` | аналитика | FBS Возвраты | `wb-ozon.js:215` |
| `GET /api/v3/warehouses` | маркетплейс | склады FBS | `wb-ozon.js:117` |
| `POST /api/v3/stocks/{складID}` | маркетплейс | FBS остатки ВБ | `wb-ozon.js:124` |
| `GET /api/v3/orders?limit=1000&next=…&dateFrom=…` | маркетплейс | Заказы ВБ FBS | `wb-ozon.js:324` |
| `POST /api/v3/orders/status` | маркетплейс | статусы заданий | `wb-ozon.js:335` |
| `POST /content/v2/get/cards/list` | контент | справочник карточек | `wb-ozon.js:156` |

`flag=0` — фильтр по `lastChangeDate`, а не по дате заказа: подхватываются и
изменения в уже выгруженных заказах.

Заголовок авторизации у всех — `Authorization: <токен>`, без `Bearer`.

### Ozon

| Метод | Задача | Строка |
|---|---|---|
| `POST /v2/analytics/stock_on_warehouses` | FBO остатки ОЗОН | `wb-ozon.js:372` |
| `POST /v4/product/info/stocks` | FBS остатки ОЗОН | `wb-ozon.js:393` |
| `POST /v3/posting/fbo/list` | Заказы ОЗОН FBO | `wb-ozon.js:696` |
| `POST /v4/posting/fbs/list` | Заказы ОЗОН FBS | `wb-ozon.js:697` |
| `POST /v1/returns/list` | Возвраты OZON (обе схемы) | `wb-ozon.js:595` |

Заголовки: `Client-Id` и `Api-Key`, оба обязательны.

### Яндекс Маркет

| Метод | Задача | Строка |
|---|---|---|
| `POST /campaigns/{кампания}/offers/stocks` | остатки FBO и FBS | `yandex-lamoda.js:42` |
| `GET /campaigns/{кампания}/orders?fromDate=…&toDate=…` | заказы | `yandex-lamoda.js:104` |

Заголовок: `Api-Key`. Кампании FBY и FBS — **разные номера**.

### Ламода

| Метод | Задача | Строка |
|---|---|---|
| `POST /rpc` → `getStocks` | остатки FBO и FBS | `yandex-lamoda.js:164` |
| `GET /v1/orders?page=…&created_from=…&fulfillment_type=…` | заказы | `yandex-lamoda.js:197` |

Заголовок обоих: `Authorization: Bearer <токен>`, но **токены разные**.

---

## 3. Переменные окружения

| Переменная | Обязательна | Для чего |
|---|---|---|
| `SHEET_ID` | да | идентификатор книги из её адреса |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | да | ключ сервисного аккаунта: сырой JSON или base64 |
| `WB_STATS_KEY` | для задач ВБ FBO | статистика: заказы и продажи |
| `WB_ANALYTICS_KEY` | для остатков FBO и возвратов ВБ | категория «Аналитика». Пусто → берётся `WB_STATS_KEY`, но у него обычно нет нужной категории |
| `WB_MARKET_KEY` | для задач ВБ FBS | остатки, задания, карточки |
| `OZON_CLIENT_ID` | для задач Ozon | |
| `OZON_API_KEY` | для задач Ozon | |
| `YM_API_KEY` | для задач Яндекса | |
| `YM_CAMPAIGN_FBY` | для FBO Яндекса | номер кампании FBY |
| `YM_CAMPAIGN_FBS` | для FBS Яндекса | номер кампании FBS |
| `LAMODA_TOKEN` | для остатков Ламоды | Seller Partner API, JSON-RPC |
| `LAMODA_B2B_TOKEN` | для заказов Ламоды | B2B Platform API, REST |
| `TZ` | нет | по умолчанию `Europe/Moscow` |
| `OZON_API_URL` | нет | **только для проб** |
| `WB_ANALYTICS_URL` | нет | **только для проб** |
| `WB_CONTENT_URL` | нет | **только для проб** |

Ключи проверяются лениво: `требовать(...)` роняет **ту задачу**, которой ключа
не хватило, а не весь сервис. Остальные задачи продолжают работать.

Сервисному аккаунту нужен доступ к книге на редактирование. Его почта — в поле
`client_email` внутри ключа.

---

## 4. Листы книги

### Задачи и листы

Обычно имя задачи совпадает с именем листа. Единственное исключение: задача
`Возвраты OZON FBS` пишет в лист `Возвраты OZON` — возвраты приходят одним
методом сразу по обеим схемам.

| Задача | Лист | Режим | Ключ (индексы колонок) |
|---|---|---|---|
| FBO остатки ВБ | тот же | снимок | — |
| FBS остатки ВБ | тот же | снимок | — |
| Заказы ВБ FBO | тот же | дозапись | `[0]` srid |
| Заказы ВБ FBS | тот же | дозапись | `[0]` задание |
| FBS Возвраты | тот же | дозапись | `[0,2]` возврат + баркод |
| FBO остатки ОЗОН | тот же | снимок | — |
| FBS остатки ОЗОН | тот же | снимок | — |
| Заказы ОЗОН FBO | тот же | дозапись | `[0,4]` отправление + артикул |
| Заказы ОЗОН FBS | тот же | дозапись | `[0,4]` |
| **Возвраты OZON FBS** | **Возвраты OZON** | дозапись | `[0]` возврат |
| FBO/FBS остатки Яндекс | тот же | снимок | — |
| Заказы Яндекс FBO/FBS | тот же | дозапись | `[0,2]` заказ + артикул |
| FBO/FBS остатки Ламода | тот же | снимок | — |
| Заказы Ламода FBO/FBS | тот же | дозапись | `[0,2]` |
| FBO/FBS остатки Мвидео | тот же | снимок | — вручную |
| Заказы Мвидео FBO/FBS | тот же | дозапись | `[0]` — вручную |

Снимок — любой лист, в имени которого есть слово «остатки» (`СНИМКИ`,
`schemas.js:84`).

### Колонки

Полные схемы — в `src/schemas.js`, раздел `СХЕМЫ`. Там же единственный источник
правды по порядку колонок; дублировать его здесь значило бы завести второе
место, где он расходится с кодом.

Два правила, общие для всех листов:

* **«Обновлено» всегда последняя.** Сравнение строк её игнорирует — иначе
  каждый прогон считал бы изменившимися все строки.
* **Правее колонок схемы лежат служебные колонки книги** — ключи окна, недели,
  месяца. Их считают формулы, сервис до них не дотягивается: ширина записи
  ограничена длиной схемы.

### Лист «Настройки»

| Что | Диапазон | Константа |
|---|---|---|
| Имя листа | `Настройки` | `НАСТРОЙКИ.лист` |
| Параметры | `A3:B12` | `параметрыОт` / `параметрыДо` |
| Блок задач | `A15:G60` | `задачиОт` / `задачиДо` |
| Куда пишется итог | `F{строка}:G{строка}` | `config.js:117` |

**Сдвигать строки на листе нельзя** — сервис читает их по номерам.

---

## 5. Числовые пределы

### Запись в книгу (`sheets.js`)

| Константа | Значение | Смысл |
|---|---|---|
| `ПРЕДЕЛ` | `50000` | докуда читаем и пишем лист. Дальше ключи не находятся и строки задваиваются |
| `ДИАПАЗОНОВ_ЗА_РАЗ` | `200` | сколько диапазонов кладём в один `batchUpdate` |
| порог предупреждения | `ПРЕДЕЛ × 0.9` | в «Результат» уходит «пора в архив» |

### Период (`http.js`)

| Константа | Значение | Смысл |
|---|---|---|
| `ГЛУБИНА_ПО_УМОЛЧАНИЮ` | `14` дней | если «Глубина дней» не заполнена |
| `ЗАПАС` | `3600000` мс (час) | на заказы во время прогона и расхождение часов |
| порог «похоже на дату» | год ≥ `2015` | отличает дату от глубины (`config.js:89`) |
| потолок серийного номера | `100000` | больше — это не дата (`http.js:118`) |
| «С даты» в будущем | `+24 ч` | допуск перед отказом (`http.js:163`) |

### Повторы (`http.js`)

| Что | Значение |
|---|---|
| попыток по умолчанию | `5` |
| пауза | `1000` мс × номер попытки |
| таймаут запроса | `120000` мс |
| повторяемые коды | `408`, `425`, `429` и всё `≥ 500` |

Остальные 4xx повтор не чинит — они летят наружу сразу.

### Wildberries (`wb-ozon.js`)

| Константа | Значение | Смысл |
|---|---|---|
| пауза статистики | `61000` мс | лимит: 1 запрос в минуту **на аккаунт** |
| пауза аналитики | `21000` мс | лимит остатков FBO: 3 в минуту, интервал 20 с |
| пауза возвратов | `61000` мс | лимит: 1 запрос в минуту |
| `ПАЧКА_ОСТАТКОВ` | `250000` | размер страницы остатков FBO |
| `СТРАНИЦ_ОСТАТКОВ` | `10` | потолок страниц |
| `КЭШ_КАРТОЧЕК` | `5 × 60000` мс | ровно на время прохода |
| страница карточек | `100` | предел метода |
| `ОКНО_ВОЗВРАТОВ` | `30` суток | предел метода — 31, берём с запасом |
| `ОКОН_МАКСИМУМ` | `24` | два года; дальше прогон растянется на полчаса |
| пачка баркодов | `1000` | в `/api/v3/stocks/{склад}` |
| пачка заданий | `1000` | в `/api/v3/orders/status` |

### Ozon (`wb-ozon.js`)

| Константа | Значение | Смысл |
|---|---|---|
| `ПАЧКА_ОТПРАВЛЕНИЙ` | `100` | **предел метода.** На 1000 отвечает 400 |
| `ЗАПРОСОВ_МАКСИМУМ` | `500` | потолок на обход одного периода |
| `ПАУЗА_OZON` | `300` мс | вежливость; `0` на подставном адресе |
| `ПАЧКА_ВОЗВРАТОВ` | `500` | предел метода |
| `СТРАНИЦ_ВОЗВРАТОВ` | `200` | сто тысяч возвратов |
| страница остатков | `1000` | |

### Яндекс и Ламода (`yandex-lamoda.js`)

| Константа | Значение | Смысл |
|---|---|---|
| `ШАГ_ЯМ` | `28` суток | метод не принимает интервал длиннее 30 |
| страница остатков ЯМ | `200` | |
| страница заказов ЯМ | `50` | |
| страница Ламоды | `500` остатки / `200` заказы | |
| пауза между страницами | `300` мс | везде |

---

## 6. Коды ответов, на которые есть особая реакция

| Код / признак | Где | Что делаем |
|---|---|---|
| `408`, `425`, `429`, `≥500` | `http.js:6` | повтор с растущей паузой |
| прочие `4xx` | `http.js:40` | сразу наружу, повтор не поможет |
| пустое тело / `204` | `http.js:46` | отдаём `{}` |
| ответ не JSON | `http.js:50` | ошибка с куском тела |
| `0` строк в снимке | `sheets.js:193` | **ошибка**, лист не перезаписываем |
| ответ без `postings` | `wb-ozon.js:452` | **ошибка**, а не «конец окна» |
| ответ без `returns` | `wb-ozon.js:623` | **ошибка**, а не «возвратов нет» |
| ответ без `report` | `wb-ozon.js:252` | **ошибка**, а не «возвратов нет» |
| курсор не двигает выборку | `wb-ozon.js:644` | **ошибка**, а не бесконечный круг |

Общее правило: **молчаливая потеря данных хуже честного падения.** Пустой
ответ, который можно спутать с «данных нет», везде превращается в ошибку — иначе
сбой у маркетплейса записался бы в книгу как успешный прогон.

---

## 7. Строки, по которым сервис узнаёт сам себя

| Строка | Где | Зачем |
|---|---|---|
| `ОШИБКА: …` | `index.js:49` | префикс «Результата» при сбое |
| `\| успех: ДД.ММ.ГГГГ ЧЧ:ММ:СС` | `config.js:102` | метка успеха, переживающая ошибки |
| `нет коннектора, данные заливаются вручную` | `index.js:36` | М.Видео |
| `ФИЛЬТР НЕ ПРИМЕНЁН: …` | `wb-ozon.js:693` | Ozon вернул возвраты раньше запрошенной даты |
| `(дата взята из колонки D)` | `wb-ozon.js:292`, `692` | «С даты» прочитана из колонки глубины |
| `, шапка проставлена` | `sheets.js:206`, `280` | дозаполнены пустые заголовки |
| `, пора в архив` | `sheets.js:262` | лист подходит к пределу |
| `, без ключа пропущено N` | `sheets.js:265` | строки без ключа не записаны |

Эти строки — часть договора с книгой: по ним человек читает, что произошло.
Меняя их, меняйте и этот список.

---

## 8. Регулярные выражения

| Выражение | Где | Что ловит |
|---|---|---|
| `/^[=+\-@]\|^0\d\|^\d{1,4}[-./]\d{1,2}[-./]\d{1,4}\|^\d{1,4}[-/]\d{1,2}$\|^\d{1,2}:\d{2}/` | `http.js:83` | значения, которые Таблицы переиначат |
| `/^\d{1,2}\.\d{1,2}\.\d{4}(?: …)?$/` | `sheets.js:92` | дата, записанная нами текстом |
| `/\\\|\s*успех:\s*(…)/` | `config.js:102` | метка успеха в «Результате» |
| `/^(\d+)/` | `config.js:97` | глубина из текста «31 д» |
| `/^https?:\/\/(127\.0\.0\.1\|localhost)[:/]/` | `wb-ozon.js:16` | подставной адрес в пробе |

Точка в первом выражении намеренно **не** ловит двухчастные значения: `3.7` и
`100.50` — обычные числа, а через `текстом()` проходят и числовые колонки.
